<% @posts.each do |post| %>
    <div class="post-wrapper">
      <h2 class="post-title">
        <%= link_to post.title, post %>
      </h2>

      <p class="created-at">
        <%= post.created_at.strftime('%B %d, %Y') %>
      </p>
    </div>
<% end %>